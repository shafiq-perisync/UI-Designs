import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        // backgroundColor: 'red',
        // padding: 10,
        width: '100%',
        alignItems: "center",
        borderRadius: 20,
        marginVertical: 10,
        height: 70,
        justifyContent: 'center',
    },
    buttonText: {
        color: '#fff',
        fontSize: 16,
    }

})